defmodule Blockytalky.DSL do
  alias Blockytalky.Lib, as: Lib

  @moduledoc """
  This is the macro-ized blockytalky language DSL
  it should be 1-1 for each of the blocks
  """
  #Ch3. pg 50: Metaprogramming Elixir
  # defmacro blockytalky(do: body) do
  #   IO.puts inspect body
  # end
  # defmacro cue(name, mappings) do
  #   IO.puts inspect mappings
  # end
  # defmacro upon  do
  #   :ok
  # end
end
