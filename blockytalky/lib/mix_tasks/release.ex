defmodule Blockytalky.Release do
  use Mix.Task
  @moduledoc """
  This is the command to be run on the raspberry pi
  It installs nginx if necessary
  It copies over the nginx file for blockytalky
  """
  def run(_) do
    #copy the nginx file
  end
end
